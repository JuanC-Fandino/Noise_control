<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.juank.noise_control">

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Particion_simple" />
        <activity android:name=".Tabla_materiales" />
        <activity android:name=".Materials_Base" />
        <activity android:name=".Particion_compuesta" />
        <activity android:name=".ParticionDoble" />
        <activity android:name=".LaminaDoble"></activity>
    </application>

</manifest>